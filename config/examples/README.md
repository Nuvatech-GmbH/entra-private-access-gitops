# Beispiel-YAMLs (nicht deployed)

Vorlagen und Demos für Private-Access-Anwendungen. Dateien enden auf `*.example.yaml` und werden von der Pipeline **nicht** nach Entra deployed.

**Neue produktive App anlegen:** Vorlage hier kopieren nach `config/applications/<name>.yaml` (ohne `.example`), Werte anpassen, PR öffnen.

Siehe auch `docs/examples/demo-application-segments.md` und das [README](../../README.md#neue-private-access-regel-anlegen--schritt-für-schritt).
